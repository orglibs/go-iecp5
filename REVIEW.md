# 项目检查记录（2026-09-20）

## 已修复

- 模块路径与 GitHub 仓库及代码导入不一致，导致 `go test ./...` 直接编译失败，也阻止正确发布。
- CS101 功能码与控制位共用一个 `iota` 分组，两个方向的功能码均偏离标准值。
- 客户端窗口值在 `uint16` 上移位，合法大窗口（如 4096）会溢出并生成零容量缓冲区。
- `SetAutoReconnect(false)` 仅在首次拨号失败时生效，连接建立后断线仍会重新拨号。
- 服务端单活动会话调度无锁遍历会话表，活动状态跨协程读写，且监视协程不响应关闭。
  现在使用会话快照、原子活动状态、串行激活确认和可取消的监视协程，移除固定 500ms 等待。
- `SetVariableNumber` 接受负数后转换为字节，可能生成错误的对象数量。
- README 文档链接和徽章引用旧地址；补充包用途、安装说明、GitHub CI 和新版本索引检查。

## 仍需关注的范围

本次检查不等同于完整的 IEC 协议认证或全部并发场景验证。已有测试和新增回归测试通过，
但下列已有设计仍需后续专项处理：

- `Client.SendQueuedASDU`、`Client.RemoteClose` 当前为空实现，`Client.AreAllMessagesConfirmed`
  固定返回 true。不能将这些接口的返回值视为发送、断开或对端确认的可靠证据；
  目前可使用 `Send`、`Disconnect`/`Close` 处理实际发送和关闭。
- 服务端持久队列模式的 `pending` 共享访问、未确认数据重入队以及多主站交接还未完整验证；
  `ServerSpecial` 仍采用旧的启动/重连生命周期。本次没有重构这些接口。
- CS101 只有基础定义，文件传输尚未实现，README 已明确说明。
- 根 `LICENSE` 是 LGPL-3.0 文本，但许多源文件头仍声明 GPL-3.0。应根据上游授权核实并统一，
  本次没有擅自更改许可证或版权声明。

## 本地验证

- Go 1.23.0 下 `make test` 通过，包含全包测试、竞态检测、`go vet` 和格式检查。
- 总语句覆盖率 57.3%；ASDU 为 68.3%，CS104 为 45.6%，不能据此推断未覆盖路径安全。
- 独立临时消费项目使用本地 `replace` 成功导入并运行三个子包；这仅验证本地可用性。
- 工作流 YAML 可解析；GitHub Actions 和公开模块下载尚未在线执行。

## 发布状态

现有本地 `v1.7.0` 含错误模块路径，应按 [RELEASING.md](RELEASING.md) 发布新补丁版本。
当前环境 DNS/网络访问受限，无法核实远端公开状态、远端标签或 pkg.go.dev 页面。
本地修改通过检查不代表已经发布或收录。
